server:
  port: 10086
  servlet:
    context-path: /eureka

spring:
  profiles:
    active: dev
  application:
    name: eureka-server

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    register-with-eureka: false
    fetch-registry: false